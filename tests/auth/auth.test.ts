// Implemented in Phase 13 — login happy/sad, /me auth guard tests
describe('Auth routes', () => {
  it.todo('POST /auth/login returns 200 + cookie on valid credentials');
  it.todo('POST /auth/login returns 401 on wrong password');
  it.todo('GET /auth/me returns 401 without token');
  it.todo('GET /auth/me returns 200 with valid token');
});
