// Implemented in Phase 8 — GET /incidents, GET /incidents/stream (SSE), PATCH /incidents/:id, GET /incidents/:id/timeline/stream
import { Router } from 'express';

const router = Router();

export default router;
